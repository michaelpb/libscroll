/*const Document = require('../../mods/document/Document');

describe('Workspace', () => {
    it('does things', () => {
        expect(1+1).toEqual(2);
    });
    it('does other things', () => {
        expect(1+1).toEqual(2);
    });
});*/
